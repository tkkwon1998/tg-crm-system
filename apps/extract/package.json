{
  "name": "@crm/extract",
  "version": "0.1.0",
  "private": true,
  "description": "Extract Worker + Cloudflare Workflow: resolve identity, call Claude, write crm_proposals (spec §5.2, §7).",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@crm/db": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250603.0",
    "typescript": "^5.5.4",
    "wrangler": "^4.20.0"
  }
}
